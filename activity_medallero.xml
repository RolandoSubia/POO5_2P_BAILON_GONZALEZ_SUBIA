<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:padding="16dp">

    <ImageView
        android:id="@+id/logoImageView"
        android:layout_width="365dp"
        android:layout_height="68dp"
        android:scaleType="fitCenter"
        android:src="@drawable/logo" />
    <TextView
        android:id="@+id/medalleroTitleTextView"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="Medallero Olímpico"
        android:textSize="24sp"
        android:textAlignment="center"
        android:layout_gravity="center"
        android:paddingBottom="16dp"/>

    <!-- Botón para ordenar por oro -->
    <Button
        android:id="@+id/ordenarButton"
        android:layout_width="272dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:backgroundTint="#0F1E6D"
        android:text="Ordenar por Oro" />

    <!-- ScrollView para deslizar el contenido -->
    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="353dp"
        android:layout_weight="1"
        android:paddingTop="16dp">

        <TableLayout
            android:id="@+id/medalleroTableLayout"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:stretchColumns="1,2,3">

            <!-- Cabecera de la tabla -->
            <TableRow>

                <TextView
                    android:layout_width="231dp"
                    android:padding="8dp"
                    android:text="País"
                    android:textAlignment="center"
                    android:textStyle="bold" />

                <TextView
                    android:layout_height="match_parent"
                    android:padding="8dp"
                    android:text="    🥇"
                    android:textStyle="bold" />

                <TextView
                    android:padding="8dp"
                    android:text="   🥈"
                    android:textStyle="bold" />

                <TextView
                    android:padding="8dp"
                    android:text="   🥉"
                    android:textStyle="bold" />
            </TableRow>

            <!-- Aquí puedes agregar más filas para el medallero -->

        </TableLayout>
    </ScrollView>

    <!-- Botón para salir -->
    <Button
        android:id="@+id/salirButton"
        android:layout_width="268dp"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:backgroundTint="#0F1E6D"
        android:paddingTop="16dp"
        android:text="Salir" />

</LinearLayout>
